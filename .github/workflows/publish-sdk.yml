name: Publish SDK to NPM

on:
  push:
    branches:
      - main
      - dev
    # paths 없으면 dev/main 푸시 시마다 실행됨. SDK 관련만 실행하려면 아래 주석 해제
    # paths:
    #   - 'src/types/**'
    #   - 'scripts/extract-types.js'
    #   - 'sdk-package/**'
  workflow_dispatch: # 수동 실행 가능

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Trusted Publishing을 위한 OIDC 토큰 발급 권한

    steps:
      - name: 📥 Checkout 코드
        uses: actions/checkout@v4

      - name: 🔧 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # registry-url 제거: Trusted Publishing(OIDC) 사용 시 토큰 기반 인증이 설정되면 충돌함

      - name: 📌 npm 11+ 설치 (OIDC Trusted Publishing 필요)
        run: npm install -g npm@11

      - name: 🔍 타입 파일 추출
        run: node scripts/extract-types.js

      - name: 📦 SDK 패키지 의존성 설치
        working-directory: ./sdk-package
        run: npm install

      - name: 🏗️ SDK 패키지 빌드
        working-directory: ./sdk-package
        run: npm run build

      - name: 📊 버전 확인 및 업데이트
        id: version
        working-directory: ./sdk-package
        run: |
          # npm에 배포된 최신 버전 가져오기
          NPM_VERSION=$(npm view byzip-v2-sdk version 2>/dev/null || echo "0.0.0")
          echo "npm 최신 버전: $NPM_VERSION"

          # 로컬 package.json 버전
          LOCAL_VERSION=$(node -p "require('./package.json').version")
          echo "로컬 버전: $LOCAL_VERSION"

          # 더 높은 버전 선택
          if [ "$NPM_VERSION" != "0.0.0" ]; then
            CURRENT_VERSION=$NPM_VERSION
          else
            CURRENT_VERSION=$LOCAL_VERSION
          fi

          echo "기준 버전: $CURRENT_VERSION"

          # 패치 버전 자동 증가
          IFS='.' read -ra VERSION_PARTS <<< "$CURRENT_VERSION"
          MAJOR="${VERSION_PARTS[0]}"
          MINOR="${VERSION_PARTS[1]}"
          PATCH="${VERSION_PARTS[2]}"
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="${MAJOR}.${MINOR}.${NEW_PATCH}"

          echo "새 버전: $NEW_VERSION"

          # package.json 버전 업데이트
          npm version $NEW_VERSION --no-git-tag-version

          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: 🚀 NPM에 배포
        working-directory: ./sdk-package
        run: npm publish --provenance --access public

      - name: ✅ 배포 완료 알림
        run: |
          echo "🎉 SDK 버전 ${{ steps.version.outputs.version }} 배포 완료!"
          echo "📦 패키지: byzip-v2-sdk"
          echo "🔗 https://www.npmjs.com/package/byzip-v2-sdk"
